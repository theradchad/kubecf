package(default_visibility = ["//visibility:public"])

load("@bazel_gazelle//:def.bzl", "gazelle")
load("//rules/workspace_status:def.bzl", "workspace_status")

gazelle(
    name = "gazelle",
    prefix = "github.com/SUSE/kubecf",
)

workspace_status(
    name = "workspace_status",
)
